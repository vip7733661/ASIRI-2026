{
  "name": "asiri-signal-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/demo-data.js && node --check src/scoring-engine.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
